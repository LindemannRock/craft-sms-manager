#!/usr/bin/env bash
#
# Pre-commit hook: runs the read-only PHPStan and ECS checks through Composer.

set -u

PACKAGE_ROOT="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)"
WORKSPACE_ROOT="$(CDPATH= cd -- "$PACKAGE_ROOT/../.." 2>/dev/null && pwd -P)"

cd "$PACKAGE_ROOT" || {
    echo "Unable to enter the SMS Manager package directory." >&2
    exit 1
}

if [ "$PACKAGE_ROOT" = "$WORKSPACE_ROOT/plugins/sms-manager" ] && [ -f "$WORKSPACE_ROOT/.ddev/config.yaml" ]; then
    if ! command -v ddev >/dev/null 2>&1; then
        echo "SMS Manager is inside the workspace DDEV project, but 'ddev' is unavailable." >&2
        echo "Install/start DDEV, then rerun the commit; host PHP is not a supported workspace fallback." >&2
        exit 127
    fi

    echo "Running SMS Manager code quality checks through the workspace DDEV runtime..."
    (
        cd "$WORKSPACE_ROOT" || exit 1
        ddev exec "cd plugins/sms-manager && php -r 'fwrite(STDOUT, \"SMS Manager pre-commit using DDEV PHP \" . PHP_VERSION . PHP_EOL);' && composer ci"
    )
    status=$?
    if [ "$status" -ne 0 ]; then
        echo "SMS Manager pre-commit checks failed in DDEV (exit $status)." >&2
        echo "Run: ddev exec \"cd plugins/sms-manager && composer ci\"" >&2
        exit "$status"
    fi

    echo "SMS Manager pre-commit checks passed."
    exit 0
fi

if ! command -v php >/dev/null 2>&1; then
    echo "Standalone SMS Manager checks require PHP, but 'php' is unavailable." >&2
    exit 127
fi
if ! command -v composer >/dev/null 2>&1; then
    echo "Standalone SMS Manager checks require Composer, but 'composer' is unavailable." >&2
    exit 127
fi

echo "Validating standalone Composer platform requirements..."
composer check-platform-reqs --no-interaction
status=$?
if [ "$status" -ne 0 ]; then
    echo "Standalone Composer platform validation failed (exit $status)." >&2
    exit "$status"
fi

echo "Validating standalone PHP quality-tool compatibility..."
php scripts/check-quality-platform.php
status=$?
if [ "$status" -ne 0 ]; then
    php_version="$(php -r 'echo PHP_VERSION;' 2>/dev/null)"
    echo "Standalone quality tools are incompatible with active PHP ${php_version:-unknown} (exit $status)." >&2
    exit "$status"
fi

echo "Running standalone SMS Manager code quality checks..."
composer ci
status=$?
if [ "$status" -ne 0 ]; then
    echo "SMS Manager pre-commit checks failed (exit $status)." >&2
    exit "$status"
fi

echo "SMS Manager pre-commit checks passed."
exit 0
